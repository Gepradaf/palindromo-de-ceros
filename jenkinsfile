pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Compilando el código...'  // Aquí podemos agregar comandos de compilación si se requieren
            }
        }
        stage('Test') {
            steps {
                echo 'Ejecutando pruebas...'  // Aquí podemos agregar comandos para ejecutar pruebas de testing
            }
        }
        stage('Deploy') {
            steps {
                echo 'Desplegando...'  // Aquí podemos agregar comandos para despliegue en la app
            }
        }
    }
}
